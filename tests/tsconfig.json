{
  "extends": "../tsconfig.base",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "../dist-tests",
    "baseUrl": ".",
    "paths": {
      "@adapters": ["../dist-adapters"],
      "@adapters/*": ["../dist-adapters/*"],
      "@src": ["../dist"],
      "@src/*": ["../dist/*"]
    }
  },
  "include": [
    "**/*",
    "../src/@types/*"
  ],
  "exclude": [
    "node_modules"
  ]
}
