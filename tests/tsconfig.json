{
  "extends": "../tsconfig.base",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "../dist-tests",
    "baseUrl": ".",
    "paths": {
      "@src": ["../dist"],
      "@src/*": ["../dist/*"]
    }
  },
  "include": [
    "**/*",
    "../src/@types/*"
  ],
  "exclude": [
    "node_modules"
  ]
}
