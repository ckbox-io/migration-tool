{
  "extends": "../tsconfig.base",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "../dist-tests",
    "baseUrl": ".",
    "paths": {
      "@adapters": ["../dist-adapters"],
      "@adapters/*": ["../dist-adapters/*"],
      "@ckbox-migrator": ["../dist"],
      "@src/*": ["../dist/*"]
    }
  },
  "include": [
    "**/*",
    "../src/@types/*"
  ],
  "exclude": [
    "node_modules"
  ]
}
